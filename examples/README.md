# Examples

These files are **generic reconstructed snippets for portfolio review**.

They are not production modules, not Commit Happens security architecture, and
not a substitute for the private application.

| File | What it shows |
| --- | --- |
| `sanitized-code-examples/result.ts` | A small `Result` type for fail-closed parsing |
| `sanitized-code-examples/format-metric.ts` | Display helpers for dashboard numbers |

Do not treat these as drop-in production source.
